server:
  port: 8080

spring:
  application:
    name: spring-ai-lyrics-extractor
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen2.5:7b

app:
  ai-worker:
    url: http://localhost:8000
